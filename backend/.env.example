# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/ecogames

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server Port
PORT=5000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Optional: Google OAuth (leave empty if not using)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: Anthropic API Key for Claude AI game generation
# Get your API key at https://console.anthropic.com/
# If not set, the AI designer will use template-based generation
ANTHROPIC_API_KEY=
