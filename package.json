{
  "name": "cla-games",
  "version": "2.0.0",
  "description": "Constraints-Led Approach game library for nogi grappling",
  "private": true,
  "scripts": {
    "dev": "cd next && npm run dev",
    "build": "cd next && npm run build",
    "start": "cd next && npm run start"
  }
}
